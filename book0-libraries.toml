# Template config for book0_api, referenced via --config on the book0-api CLI.
#
# Paths are env-var placeholders (${VAR_NAME}), not real filesystem paths -
# this file is safe to commit as-is. Set the referenced env vars before
# starting the server; each is substituted in at load time. A tag whose
# placeholder references an unset env var makes the server fail to start
# (fail fast, rather than silently serving a broken library).
#
# Each value is a library's directory (the same syntax book0_cli's
# .book0.toml uses) - book0_api appends metadata.db itself, just like book0.
#
# Add one line per library you want to expose, then point book0-api at this
# file:
#   FICTION_LIBRARY_PATH=/path/to/fiction \
#   WORK_LIBRARY_PATH=/path/to/work \
#   uv run book0-api --config book0-libraries.toml --reload

[libraries]
fiction = "${FICTION_LIBRARY_PATH}"
work = "${WORK_LIBRARY_PATH}"

# Optional: name one of the tags above as the server-side default. When set,
# clients (book0-remote) may omit --tag and the server resolves this tag on
# their behalf; without it, an omitted --tag is rejected with a 400
# TagRequiredError.
# default-library = "fiction"

# Optional: cap every response's page size at this value, and force pagination even when a
# client sends no page_size param at all (a client requesting a smaller page_size still gets
# its own smaller size - this is a ceiling, not a fixed size). Protects the server from an
# unbounded query when unset, no cap is imposed and an omitted page_size stays unpaginated,
# exactly like today.
# default-page-size = 100

# Optional: PostgreSQL DSN. When set, every route is served from the PG
# catalog (calibre_pg_sync schema) instead of the SQLite metadata.db paths
# above - the tag resolves to libraries.name and the `libraries` mapping is
# ignored. Leave commented to keep serving SQLite files.
# pg-dsn = "postgresql://postgres:…@localhost:5432/grimoire"
